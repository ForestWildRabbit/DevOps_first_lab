name: Building

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]


jobs:
  build:
    env:
      ARTIFACTS_DIR: '/home/runner/work/DevOps_first_lab/DevOps_first_lab/dist/'

    runs-on: ubuntu-latest

    steps:
      - name: checkout repository content
        uses: actions/checkout@v2

      - name: setup python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Build
        run: |
          pyinstaller -F main.py

      - name: Run
        run: |
          cd dist
          chmod +x main
          ./main https://www.youtube.com/watch?v=vx2u5uUu3DE

      - name: Check audio
        run: |
          ls -la
          

      - name: Upload a Build Artifact
        uses: actions/upload-artifact@v3
        with:
          name: my-artifact
          path: ${{env.ARTIFACTS_DIR}}
